services:
  - type: web
    name: mart-labaco-portfolio
    env: node
    buildCommand: npx --yes pnpm@9 install --no-frozen-lockfile && NODE_ENV=production npx pnpm@9 --filter @workspace/portfolio run build
    startCommand: NODE_ENV=production npx pnpm@9 --filter @workspace/portfolio run serve
    envVars:
      - key: NODE_ENV
        value: production
